# Process Simulation Project

## Overview

This project simulates the lifecycle of processes through various states: New, Ready, Running, Waiting, and Terminated.

## Files

- `main.py`: Entry point to run the simulation.
- `process.py`: Contains the `Process` class and its state transitions.
- `state.py`: Defines the `ProcessState` enumeration.
- `utils.py`: (Optional) Placeholder for utility functions.

## Requirements

Make sure to have Python 3.6+ installed. Install the necessary packages using: